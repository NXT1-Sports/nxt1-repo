/**
 * @module @nxt1/ui/components/entity-page-header
 *
 * Shared structural layout shell for all entity page headers
 * (athlete, team, college, organization). Uses content projection
 * so each consumer provides variant-specific content while the
 * layout (row flex, gap, padding, alignment) stays consistent.
 */

export { NxtEntityPageHeaderComponent } from './entity-page-header.component';
