/**
 * Components barrel export
 * @module @nxt1/ui/components
 *
 * Presentational components only - no business logic dependencies.
 * Components that need services (e.g., offline indicator) belong in app layer.
 */

export { NxtRefreshContainerComponent, type RefreshEvent } from './refresh-container';
